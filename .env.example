PAYLOAD_TEST_USER_EMAIL=user@btnopen.test
PAYLOAD_TEST_USER_PASSWORD=local
PAYLOAD_MCP_E2E_API_KEY=

# Clerk brokers GitHub sign-in for blog comments. Leave empty to run without
# comment auth (the composer degrades to a disabled state). In Vercel, scope the
# development-instance keys to Preview and the production-instance keys to
# Production.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
