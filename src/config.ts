export const urlOrigin = `https://${process.env.NEXT_PUBLIC_VERCEL_URL ?? "localhost:3000"}`;

export const hashnodePublicationHost = "btnopen.com";

export const sentryDsn = process.env.NEXT_PUBLIC_SENTRY_DSN;

export const mixpanelToken = process.env.NEXT_PUBLIC_MIXPANEL_TOKEN;
